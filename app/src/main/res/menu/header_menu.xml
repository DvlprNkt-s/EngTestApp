<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

<group android:id="@+id/first_group" android:checkableBehavior="single">
    <item android:id="@+id/home_btn_menu"
    android:title="@string/home_menu"
        app:showAsAction="never" />
    <item android:id="@+id/tests_btn_menu"
        android:title="@string/tests_menu"
        app:showAsAction="never"/>
    <item android:title="@string/technologies"
        android:id="@+id/techologies_btn_menu"
        app:showAsAction="never"/>
</group>
</menu>